"""API Services Package.

This package contains service classes that enable agents to communicate with external services
through gateway + IDP authentication. Each service class is designed to interact with a specific
external API and provides methods for common operations related to that service.

Each service inherits from the ServiceAPIBase class, which provides common functionality
for making API calls and handling authentication.
"""
